"use client";

import { Card, CardContent } from "@/components/ui/card";

export default function DepartamentosPage() {
  return (
    <Card>
        <CardContent className="p-8 flex justify-center items-center h-96">
            <p className="text-muted-foreground">Esta página está sendo preparada para novas funcionalidades.</p>
        </CardContent>
    </Card>
  );
}
